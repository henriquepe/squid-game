export interface Colors {
  primary: {
    light: string;
    base: string;
    dark: string;
  };

  gray: {
    100: string;
    200: string;
    500: string;
    700: string;
    900: string;
  };

  white: string;
}

export interface Font {
  smallRegular: {
    fontFamily: string;
    fontWeight: string;
    lineHeight: string;
    size: string;
  };

  bigRegular: {
    fontFamily: string;
    fontWeight: string;
    lineHeight: string;
    size: string;
  };

  smallBold: {
    fontFamily: string;
    fontWeight: string;
    lineHeight: string;
    size: string;
  };

  intermediateBold: {
    fontFamily: string;
    fontWeight: string;
    lineHeight: string;
    size: string;
  };

  bigBold: {
    fontFamily: string;
    fontWeight: string;
    lineHeight: string;
    size: string;
  };
}
